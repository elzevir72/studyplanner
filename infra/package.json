{
  "name": "study-planner-infra",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "deploy": "serverless deploy --stage prod",
    "deploy:dev": "serverless deploy --stage dev",
    "remove": "serverless remove --stage dev"
  },
  "devDependencies": {
    "serverless": "^3.39.0"
  }
}
