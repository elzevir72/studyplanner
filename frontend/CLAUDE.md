# Frontend — 가이드

React + TypeScript SPA. S3 + CloudFront로 정적 호스팅. API 계약은 [../docs/api.md](../docs/api.md) 참고.

## 원칙
- **모바일 우선 반응형** — 매일 짧게 기록하는 습관형 도구라 모바일 사용 비중이 높을 것으로 예상. 네이티브 앱은 만들지 않고(스토어 배포/보안 검토 부담) 반응형 웹으로 모바일 사용성을 최우선으로 설계한다.
- 화면은 최소 4개로 시작: (1) 사용자 선택 + PIN 입력, (2) 개인 기록 입력/조회 (목표 설정/수정, PIN 변경 UI 포함), (3) 그룹 대시보드, (4) 관리자 화면(`/admin`, 참가자 흐름과 별도 경로 — 메인 네비게이션에 링크 노출 안 함).
- 사용자 선택 화면(1)의 드롭다운은 `status="active"`인 유저만 보여준다.
- 개인 기록 화면은 하루에 여러 학습 수단(method)을 각각 다른 내용(topics)·학습량(amount)으로 기록할 수 있어야 한다 (예: 인강으로 문법·청해 30분, 문제집으로 어휘 5페이지) — 수단 블록을 추가/삭제하는 UI. 목표(`daily_goal`)도 수단별로 여러 개 설정 가능하며, 기록 입력 시 수단별 달성률의 평균을 달성률(%)로 보여준다. 단위가 목표와 다르거나 목표 자체가 없는 수단은 "달성률 계산 불가" 정도로 자연스럽게 표기(에러 아님). PIN 변경 기능도 이 화면에 둔다.
- 수단(method)마다 학습량 입력 방식이 다르다(`EntryPage.tsx`의 `METHOD_AMOUNT_KIND`) — 인강은 시간/분, 모의고사는 맞은 개수/전체 문항 수(분수형, unit="문항" 고정), 문제집은 페이지 수(단위 자동 고정), 단어암기는 개수(단위 자동 고정). 프리셋에 없는 수단을 직접 입력하면 수량+자유 단위로 입력받는다. 이 매핑은 UI 표시 방식일 뿐이고, 저장되는 데이터는 항상 `{value, unit}` 형태로 동일하다.
- 기록 저장(`기록 저장` 버튼) 성공 시 "저장이 완료되었습니다" 알림을 띄우고 그룹 대시보드로 이동한다 — 저장 후 바로 그룹 현황을 확인하는 흐름을 기본으로 한다.
- 대시보드에는 현재 시즌 이름/기간을 표시하고, 주/격주/월간 뷰 외에 "이번 시즌 전체" 뷰도 제공한다. 참가자별 달성률(%)을 `entry_count`와 함께 보여주되, 개인 비교/순위 매기는 톤은 피하고 담백하게 표기한다.
- 대시보드와 개인 기록 화면 상단에 현재 시즌의 `exam_date` 기준 D-day 배너("OO 시험까지 OO일 남았습니다")를 표시한다. `exam_date`가 없으면 배너 자체를 숨긴다.
- 관리자 화면(4): 참가자 계정 생성(초기 PIN 지정)/상태 변경, 시즌 생성/전환 폼을 제공. 참가자용 인증 상태와 완전히 분리해서 관리한다(같은 브라우저에서 참가자 로그인 중이어도 서로 영향 없어야 함).
- 상태 관리 라이브러리는 규모상 과설계 금지 — React 기본 상태 + fetch로 충분하면 Redux 등 도입하지 않는다.
- 로그인 토큰은 짧은 TTL이므로 만료 시 자연스럽게 사용자 선택 화면으로 리다이렉트되도록 처리.
- 대시보드에서 "미수행자" 등 그룹 정보는 압박감을 주지 않는 톤으로 표기한다 (이 도구의 목적은 성과 관리가 아니라 공유 촉진).

## 구현 현황 (2026-07-23 기준)
스캐폴딩 완료 — Vite + React + TypeScript. `src/pages/`(로그인, 개인 기록, 대시보드, `AdminPage`), `src/components/DdayBanner.tsx`, `src/api/client.ts`(백엔드 API 계약과 매핑 확인됨), `src/auth.ts`(참가자/관리자 토큰 분리 저장), `src/achievement.ts`(달성률 표시 로직) 모두 존재. S3+CloudFront로 배포되어 실사용 검증 단계.

## 아직 없는 것
없음 — 설계된 4개 화면 모두 구현됨. 이후 변경은 실사용 피드백 기반 조정 위주.
